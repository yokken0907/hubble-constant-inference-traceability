# Repository utilities

The Python files in this directory provide small, dependency-free integrity and navigation utilities for this repository.

```bash
python tools/verify_publication_package.py
python tools/trace_record.py N001
```

Python source files under `tools/` are licensed under the MIT License; see [`LICENSE`](LICENSE). This separate code license does not alter the CC BY 4.0 terms that apply to the repository's documentation, tables, data files, provenance registers, and audit records.
