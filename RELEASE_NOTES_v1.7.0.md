# Hubble Constant Inference Traceability v1.7.0 - final local package

This release adds two bounded project-internal computational replay records without changing the original 30 scientific statements, 46 principal numerical results, or V001.

- **E001:** fixed-seed 1,000-draw CMB amplitude-bootstrap replay, N025-N026, `COMPLETE_WITH_SCOPE`.
- **E002:** two-layer HTS59-HTS67 posterior-attribution replay, N029-N035, `COMPLETE_WITH_SCOPE`.

E002 does not redistribute third-party posterior exports. HTS59-HTS65 are replayed from 51 official, hash-fixed selected members. HTS66 uses verified path-sanitized portable replicas of the historical intermediate inputs required by its fixed gates. The Phase2C official empty-cache run is the sole current acceptance lineage. Its eight designated HTS67 substantive tables are byte-identical to the preserved historical substantive reference. The failed historical HTS66 HOLD archive is excluded.

Neither replay reconstructs originating likelihoods, samplers, convergence assessment, or posterior-generation processes, and neither is external independent validation.

## Reproducibility repair

E002 now verifies the just-generated output directory rather than a retained acceptance table. The repaired replay directly recovered N029-N035, the expected HTS66 classification, and the expected HTS67 convention-sensitivity classification. HTS67 uses a verified 51-member compatibility cache view. Each stage separately preserves historical checksums and verifies current portable files.

The intermediate archives required by HTS66 are distributed as verified path-sanitized portable replicas; their substantive scientific members retain byte identity. The current `HTS67_HISTORICAL_VS_FRESH_COMPARISON.tsv` is generated from the retained Phase2C result ZIP and records 8/8 designated substantive tables as byte-identical. The earlier non-byte-identical comparison is preserved unchanged under `historical_earlier_replay/` and is not current E002 acceptance evidence.

Document synchronization, complete root manifest/checksum regeneration, and final-package verification are complete for this local v1.7.0 package. Phase2C completed official acquisition from a newly empty external cache: the FIXED archive matched its full SHA-256, all 51 selected members matched their registered hashes, and E002 passed. The ORIGINAL archive was accessed by HTTP Range and was not fully materialized; its ETag mismatch is recorded as HTTP metadata rather than an input-identity failure. Creation of a remote Git tag, GitHub Release, persistent identifier, or Jxiv record was not performed and must not be inferred from this package.
